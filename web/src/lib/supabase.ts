/**
 * Supabase client will live here.
 * Install `@supabase/supabase-js` when ready, then create the browser client
 * using NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY.
 */
export {};
