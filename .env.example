# Copy values into web/.env.local and backend/.env as needed.
# See each app's .env.example for the full list.

# --- Shared / reference ---
# Frontend (Vercel): web/.env.local
# Backend (Railway): backend/.env

# Clerk keys from https://dashboard.clerk.com (API keys).
# Placeholders only — put the real values in web/.env.local and backend/.env, never commit them.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
